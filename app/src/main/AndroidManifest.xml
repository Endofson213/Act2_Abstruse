<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="mcm.edu.ph.act2_abstruse">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Act2_Abstruse">
        <activity android:name=".GameoverScreen"></activity>
        <activity android:name=".Result" />
        <activity android:name=".Wrong3" />
        <activity android:name=".Wrong2" />
        <activity android:name=".Wrong1" />
        <activity android:name=".Lead3" />
        <activity android:name=".Lead2" />
        <activity android:name=".Lead1" />
        <activity android:name=".MainActivity"/>
        <activity android:name=".MainScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>